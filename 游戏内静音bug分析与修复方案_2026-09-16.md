# 「游戏内静音」Bug 分析与 BVF 修复方案

> 日期：2026-09-16 ｜ 游戏版本：PEAK v2.4.c ｜ BVF 版本：1.1.2
> 关联模组：CrossplayStutterFix 1.1.0（Rombusz）
> 反编译归档：`MODs\反编译\CrossplayStutterFix_1.1.0_2026-09-16\`

## 一、结论速览

- PEAK v2.4.c 官方修的"长局卡顿"= **删掉刷屏的调试日志**，底层 bug 没修。
- 底层 bug 在 v2.4.c 的表现从"卡顿"变成**"本机永久静音某队友"**。
- CrossplayStutterFix 仍然有效，建议保留（修了根因+三个空引用守卫）。
- BVF 面板当前会把中招者显示为绿色「已连接」，存在诊断盲区。
- 拟为 BVF 增加：检测 + 面板标红 + （可选）直接回写修复。**纯本地生效，不需要对端/主机安装。**

## 二、Bug 机理（人话版）

### 2.1 名牌柜比喻

游戏为每个玩家维护一张"名牌"（`CharacterData`：死活、静音、能否通话等状态），并用一个静态字典按玩家登记：

```csharp
private static Dictionary<Player, CharacterData> m_instances;  // 名牌柜
```

### 2.2 柜子管理的两个缺陷（v2.4.b / v2.4.c 均存在，官方未改）

`CharacterData.Awake()`（角色出生）：

```csharp
m_instances.TryAdd(m_photonView.Owner, this);   // 旧名牌还在 → 静默失败，新名牌挂不上
```

`CharacterData.OnDestroy()`（角色销毁）：

```csharp
m_instances.Remove(m_photonView.Owner);          // 无条件清空——哪怕格里已换成新名牌
```

**发作时机**：玩家复活 / 被传送 / 换场景重生角色的瞬间，新旧两个角色对象短暂共存 → 上面两步按错误顺序执行 → 该玩家在柜子里的格子永久丢失，`TryGetPlayerData` 从此返回 false。

### 2.3 从丢名牌到静音

每个角色挂一个 `CharacterVoiceHandler`（管理该玩家在你机器上的语音 AudioSource）：

```csharp
// Start() —— 只在出生那一刻查一次柜子，终身不重查
m_character.TryGetGlobalPlayerData(out m_characterData);

// Update() —— 每帧
bool mute = m_characterData == null || m_characterData.isMuted || m_characterData.isBlocked;
audioSource.mute = mute;
```

名牌柜丢条目 → `m_characterData` 缓存为 null → 该玩家的 `audioSource.mute` 被焊死为 true。

**关键：语音数据流其实一直在到本地**（Photon Voice 连接层完全正常），只是游戏不播。这就是"BVF 面板全绿但听不到人"的根因。

### 2.4 v2.4.b 时代为什么表现为卡顿

同一条失败路径上，v2.4.b 的 `AsyncCheckCommunicationWithUser` 协程会**每秒写一条 `LogError`（带完整堆栈）直到永远**，丢的人越多刷得越凶 → "长局突然开始卡"。v2.4.c 官方把这条日志删了，卡顿消失，静音保留。

## 三、本地问题还是主机问题

**纯本地。** 名牌柜每台机器各有一份，丢不丢、丢谁的，由各端自己的生成/销毁顺序决定。

- 触发时机常与主机行为相关：CSF 1.1.0 修的已知案例——"主机在远征载入界面停留超过 10 秒 → 所有客机同时丢主机名牌 → 全员听不到主机"。看似主机故障，实为每台客机各自撕错了名牌。
- 你听不到 ≠ 队友也听不到。

## 四、修复能做到什么程度

| 层级 | 做法 | 效果 |
|---|---|---|
| 检测 | 遍历 `Character.AllCharacters`，找 `voiceHandler.Initialized && m_characterData == null` 的远端非 bot 角色 | 面板标出"此人被你本机静音" |
| 修复 | 角色对象身上的 `character.data` 一直都在（不经过柜子）→ 直接回写 `vh.m_characterData = ch.data` | **当场解除静音**，比 CSF 修柜子的路线更直接——柜子烂着也能修 |
| 局限 | 只修"听不到"这一个症状 | 柜子空了还影响其他走 `TryGetPlayerData` 的路径（屏蔽状态同步等），不在本面板职责内 |

字段可达性：csproj 已挂 `BepInEx.AssemblyPublicizer`，`private m_characterData` 可直接读写，零反射。

## 五、需不需要主机/对端安装

**不需要。** 谁装谁修自己的耳朵：你装了 → 你这边中招的人全部恢复可听；主机那边听不到你 → 是主机自己机器的柜子问题，得他自己装。无版本对齐依赖（与 BVF 主机广播功能"两边都要装"的性质不同）。

## 六、BVF 面板表现设计（按现有布局）

静音 bug 与连接状态是**正交的两层**——`[同步]` 的人照样可能被静音。故不动 Verdict 状态 tag（动了会破坏 `同步:n/n` 计数语义），诊断走现有 `»` 副行：

```
[同步] 梦乃 | 212ms - 236ms
    » 游戏内静音·原版bug(重连无效)

[跨区] CrossRegionSample | 420ms - 445ms
    » 连接的语音服: jp 日本×(1.2.3.4:5056)  [内静音]
```

实现要点：

1. `PlayerClass` 增加 `bool InGameMuted`
2. `ClassifyPlayers()` 开头建 `HashSet<int> muteBugActors`（一次 `AllCharacters` 遍历，≤12 角色，开销可忽略）
3. `needLine2` 条件补 `|| d.InGameMuted`；`BuildLine2` 对已有内容的行追加 `[内静音]`，对正常状态行单独成行
4. 颜色用 `C_RED`（现有语义：听不见 = 红）
5. 顶部汇总行可选追加 `[内静音:n]`（仅 n>0 时）
6. 每 actor 写一条 `DiagLog` 便于排查
7. 本地化：`line2_ingame_muted` → 中文「游戏内静音·原版bug(重连无效)」/ English「Muted in-game (vanilla bug, reconnect won't help)」

排除项（防误报）：

- 本机 `IsLocal` —— 自己的 mute 不影响收听
- `isBot` —— bot 无真实语音
- `photonView.Owner == null` —— 无主对象
- `Initialized == false` —— Start() 未跑完的暂态
- `isMuted`/`isBlocked` 非空但为 true —— 合法屏蔽，不算 bug

## 七、可选档位

| 档位 | 内容 | 性质 |
|---|---|---|
| A | 检测 + 面板显示 + 自动回写修复（配置开关，默认开） | 修症状，写游戏字段 |
| B | 仅检测 + 面板显示 | 纯只读，最保守 |

两档都与 CrossplayStutterFix 兼容共存（CSF 防丢条目；BVF 修已缓存 null 的既成事实，覆盖 CSF 够不着的残案）。

## 八、证据清单（本次核查所用）

| 项 | 位置 |
|---|---|
| CSF 模组完整反编译 | `反编译\CrossplayStutterFix_1.1.0_2026-09-16\CrossplayStutterFix\` |
| v2.4.c 六个目标类型对照 | 同目录 `v2.4.c_*.cs` |
| v2.4.b 全量对照 | `反编译\Assembly-CSharp_PEAK_2026-09-03_v2.4.b\` |
| 原版缺陷代码 | `CharacterData.Awake/OnDestroy`（TryAdd + 无条件 Remove）、`CharacterVoiceHandler.Start/Update`（单次缓存 + mute 判定） |
| 官方 2.4.c 改动 | 删除 `AsyncCheckCommunicationWithUser` 每秒 LogError、`Record` 的 `"RECORD REVIVED!"`、`WarpPlayer` 的 LogError+4 处 Log、`RPCA_Fall` 的 Log、`Start` 的 allowedToCommunicate Log |

---
状态：待用户拍板（档位 A / B / 不做）
