# BetterPeakVoiceFix 变更报告：1.0.5 → 1.6.1

会话日期 2026-09-05 ~ 09-06 · 游戏版本 PEAK 2.4.b (buildid 25090101) · 源码 `MODs/PeakVoiceFixFix/`

---

## 0. 一句话总结

起点是"PEAK 更新后语音修复 mod 显示全红但语音其实能用"。查下来**故障主体是我们自己的 bug**，不是本体：mod 重连语音时既没带区服、也没等断开完成，于是每次"修复"都只是把语音踢下线然后连不回来。顺带查实了本体三个真 bug（区服表漏 hk、切区不等主服务器就加房、邀请握手无超时），都做了幂等兜底。面板按用户重新设计的两套样式重写。

---

## 1. 起因与原始症状

群里反馈 + 用户截图（mod v1.0.5）：

```
本机已连服务器: [断开]
房主已连服务器: [ylsword] 185.14.47.69:5056
[本机] 雪檐        | 延迟:100ms
[异常] ★ ylsword   | 延迟:56ms
[异常] 乌鸦吃鱼     | 延迟:50ms
本机语音: 断开     语音连接人数: 0/3
```

关键矛盾：**语音实际能听见，但面板说断开**。同伴 LengSword（BetterRoomShare 作者）给的线索是"photon 那块改了，进不同区域的房就能复现"。

---

## 2. 坐实的技术发现

### 2.1 我们自己的 bug（全部已修）

| 编号 | 问题 | 后果 |
|---|---|---|
| A1 | 重连时把全局 `AppSettings` 直接传给 `ConnectUsingSettings`，`FixedRegion` 为空 | 语音客户端去连"自己缓存的最佳区"而不是游戏当前区服。**这是分叉的直接原因** |
| A2 | `Disconnect()` 与 `ConnectUsingSettings()` 写在同一帧 | Photon 要求 `PeerState == Disconnected` 才允许连接，而 `Disconnect()` 是异步的 → **每次重连都被静默拒绝**，等于只把语音踢下线 |
| A3 | 不拷 PUN 的 `AuthValues` / `AuthMode` / `EncryptionMode` / `SerializationProtocol` | 语音客户端缺 UserId，语音房身份匹配退化 |
| A4 | 传的是全局 `AppSettings` **引用** | `VoiceConnection` 会往里写 `BestRegionSummaryFromStorage`，污染游戏全局设置 |
| A5 | 反射写 `GameServerAddress` 想"强制跟随房主" | 该值在 `ConnectUsingSettings` 流程里会被 MasterServer 的 join 响应覆盖（`LoadBalancingClient:1620`），只有 `ReconnectAndRejoin()` 会采纳它，而 mod 从不调用 → **这个动作从未生效** |
| A6 | `GameObject.Find("VoiceClient")` 找语音客户端 | 只搜激活对象，且日志证实存在多实例竞争（`Already Found VoiceClient, Destroying...`） |
| A7 | 换房不清 `punVoice` 引用 | 可能攥着上一局已弃用的对象 |
| A8 | `PVF_Ping` 的发布依赖 `punVoice` 可用 | 出故障的人在队友界面上退化成"没装 mod"，把故障藏起来 |
| A9 | `Disconnected` 分支无放弃阈值 | 连不上时每 8 秒无限重试 + SOS 刷屏 |
| A10 | 日志导出写 `BepInEx/Log/`（该目录不存在） | 按钮永远失败 |
| A11 | 踢重连后未重设语音兴趣组 | 存在"连着但没声音"的隐性风险（`VoiceClientHandler.InitNetworkVoice` 的订阅是有条件的） |

### 2.2 本体的 bug（已兜底 / 已记录）

**B1 区服编码表漏了 hk。** `Portningsbolaget.Utilities.Utilities.REGIONS` 只有 15 项（us/usw/ussc/eu/au/za/asia/cae/in/jp/sa/kr/tr/ru/rue），**没有 hk**，却多收了 Photon 压根没给 PEAK 开通的区。`RegionToCode("hk")` 落 fallback `'-'`，而 `CodeToRegion('-')` 里 `Mathf.Clamp(45-65, 0, 14) = 0` → **解出 `us`**。后果：港服房的房间码给别人 → 对方被切到美国区找一个在香港的房间 → 必然失败。实机证据：房名 `-DRGPR`，同行日志写着 `region hk`。

**B2 切区后立刻加房。** `MainMenuJoinRoomPage.SwapRegionAuthentication` 是 `Disconnect()` → `ConnectToRegion()` → **立刻** `JoinRoom()`，此时还在连主服务器。本体自己写了 `WaitForPhotonState` 协程却**从未调用**（死代码）。

**B3 Steam 邀请的房间号握手没有兜底。** 客机往大厅广播"要房间号"，房主只在 `IsMasterClient && InRoom` 时才回。客机发出请求后置上 `m_currentlyRequestingRoomID` 就**永不重发、无超时、无提示**——房主当时在菜单/加载中/刚掉线，客机永久静默卡在主菜单。

**B4（不必修）** 切区服不重新认证：`NetworkConnector.SwapRegionAuthentication` 会重新取认证再切区，但它**是死代码**（无调用点）。不过 PEAK 的 Photon 认证是 `CustomAuthenticationType.None` + UserId（见 `NetworkingUtilities.LoadUserID`），不带 Steam 票据，所以重新认证意义不大。

**B5（噪音）** `Invalid Steam Lobby: Missing Room Data`：`SteamRuntimeManager.JoinLobbyRoutine` 读大厅键 `PhotonRoom`，而全局无任何代码写这个键 → 每次接受邀请必报错。主机移植路径的死代码，无害。

### 2.3 环境事实（实测，不是推断）

**PlayerPrefs 里有两条独立的最佳区缓存**（注册表 `HKCU\Software\LandCrab\PEAK`）：

```
PUNCloudBestRegion   => eu;224;asia,au,eu,hk,jp,sa,us,ussc,usw
VoiceCloudBestRegion => ussc;200;asia,au,eu,hk,jp,sa,us,ussc,usw
```

游戏在 **eu**，语音的缓存却是 **ussc**。这是 A1 的放大器：只要重连不钉 `FixedRegion`，语音就会读自己那条缓存跑去美国。**所以分叉根本不需要跨区房——单人自己开房按一下 Alt+K，1.0.5 就会把语音踢到美国。**

第三段是 Photon 后台**实际给 PEAK 开通**的区服列表，**hk 在里面**（印证 LengSword 的说法）。另有实机日志 `Calling OpJoinOrCreateRoom for room name '-DRGPR_voice_' region hk.` 证明语音在 hk 能正常连接和加房。

裸连（无加速器）测速结果：eu 224ms、ussc 200ms，**hk 虽然可用却没被选中**——国内到香港 Photon 服务器的实测比欧美还差，不是 hk 不能用。

### 2.4 中途纠正过的三个结论（重要）

1. **Steam 认证不是故障原因。** 我起初把日志里 `Steam auth ticket received` 认成 Photon 认证，实际那是 EOS 登录在用 Steam 票据换 token（下一行就是 `EOS auth start (steam)`）。`SteamPhotonAuthManager` 的唯一调用点是死代码。
2. **区服不是唯一的分叉变量。** Photon 的 `Region` 有 `Cluster` 字段，同 region 可有多个集群；换房时语音也可能还挂在旧房名上。**所以 ✓/✗ 判据必须是 IP 比较，区服只是给人看的解释。**
3. **房主掉线不会导致全队断线。** Photon 是服务器中继，房间在服务器上，房主只是逻辑角色（MasterClient）。卡麦是个体事件。

### 2.5 另一条独立的故障链：永久卡麦

与区服分叉无关、且更常见：

```
① 网络抖动 → 语音客户端 ClientTimeout
② 库自动调 ReconnectAndRejoin()，加入模式是 RejoinOnly
③ 但语音房 PlayerTtl 只有 2000 毫秒，断线超过 2 秒占位就没了
④ RejoinOnly 失败 → 返回码 226
⑤ 库置 errAuthOrJoin = true; manualDisconnect = true; Disconnect();
⑥ FollowLeader() 开头判断 (errAuthOrJoin && !IsConnected) 直接 return
   → 本体自动重连彻底停摆，一直哑到换关卡
```

**本体压根没有能力发现语音分叉**：`FollowLeader()` 的自检只比语音房名（全员相同），不比区服。所以分叉一旦发生，没装 mod 的人一直错到退房。mod 的"检测 + 用正确参数重连"是唯一修复手段。

---

## 3. 版本变更明细

部署方式：每版**新建文件夹**（`BepInEx/plugins/0核心&基础模组/[优化]更好的语音修复CN-<版本>`），旧文件夹保留，BepInEx 按 GUID 比较版本号自动加载最高版、跳过其余（日志可见 `Skipping [BetterPeakVoiceFix 1.x] because a newer version exists`）。回退 = 删掉高版本文件夹。当前并存 1.2.1 / 1.3.0 / 1.4.0 / 1.5.0 / 1.6.0 / **1.6.1（生效）**。

### 1.1.0 — 连接核心（已实机验证）

修 A1–A11。核心是新增一个"照本体方式连语音"的路径：拷 `AppSettings` 副本 → 钉 `FixedRegion = PhotonNetwork.CloudRegion` → 拷 AuthValues/AuthMode/EncryptionMode/SerializationProtocol → 分帧等断开完成（5 秒超时）→ 连接 → 重设兴趣组。删掉反射写 `GameServerAddress`。

**实机验证结果**（决定性）：三次语音加房日志全是 `region eu`，而语音自己的缓存是 `ussc`——旧版必然会出现 `region ussc`。且日志中**没有** `Can only connect while in state 'Disconnected'`（旧版每次重连必现），`VoiceCloudBestRegion` 的值一字未变（证明 A4 生效）。

### 1.2.0 / 1.2.1 — 区服控制

- 新增「强制区服」配置（`auto` + Photon 实际开通的 9 个区），patch `PhotonNetwork.ConnectUsingSettings()` 无参重载在连接前写入 `FixedRegion`。**作用范围仅限自己开房和主菜单连接**——加别人的房时本体会切到房主区服，不干预（干预就进不去房）。
- 强制的区 20 秒连不上 → 日志 + 屏幕提示（允许在主菜单显示），**不自动改配置**。
- 新增「延迟质量体检」：连上后 8 秒取实测 ping，与缓存里自动选区的成绩比，明显更差（+60ms）或绝对值 ≥300ms 时提示。实测触发场景：强制 hk 得到 420ms，而自动选区 eu 只有 224ms。
- Alt+J 控制台新增「区服状态」「测各区延迟」两个按钮。
- **1.2.1 修的 bug**：测速原本用独立 `LoadBalancingClient` 但没设 `AuthValues`，PEAK 用 `CustomAuthenticationType.None` + UserId，裸客户端过不了名称服务器认证 → 区服列表永远为 null → 只能超时。改为**优先借用 PUN 自己的 `RegionHandler`**（已有区列表，只让它多发一轮 UDP ping），独立客户端仅在 PUN 未连接时使用且自带随机 UserId。超时从 30s 放宽到 45s，并输出诊断与已测到的部分结果。

### 1.3.0 — 面板改版（第一版）

区服为主、IP 降级到专业模式；新增 `PVF_VR`（语音区服）/ `PVF_VP`（语音延迟）两个广播键；双延迟显示；跨区人数聚合；属性广播间隔 89s → 30s。

### 1.4.0 — 面板行为修正

- **修"机场里简易 UI 死活不出来"**：场景名原本靠 `activeSceneChanged` 事件跟踪，PEAK 的加载方式不保证触发；且「菜单界面时隐藏」用 `Cursor.visible` 判断，而机场里光标本来就可见 → 整个机场被判成"菜单开着"。改为每帧直读场景名 + 机场例外。
- J 键改三态循环：关闭 → 简易 → 详细。
- 配置分区重排：显示类 5 项移入「UI设置」；「网络设置」与「区服」两个分区并入「高级与调试」。

### 1.5.0 — 面板重构（按用户定稿）

- 详细模式顺序：标题 → 区服行 → `[同步:n/N] [异常:m]` → 分界线 → 列头 → 玩家列表 → SOS → 缓存快照。删掉底部重复的三行。
- 「房间语音服」四级降级取值，**只信别人的自报**：多数派(≥2) → 房主 → 单人上报 → 本机推定(标注) → 未知。本机自己的地址永不作为权威答案。
- 无人上报时，"语音房里只有我、游戏房有别人"仍是铁证，可判 `× 本机孤立`。
- 第二行：本机始终有；其余只在等待/跨区/断开时出（同步与错位不出，错位≈连接正常）。本机连错时 IP 无视专业模式强制显示。
- 本机语音断开时，没装 mod 的队友那行明说"未知：本机断开语音服且对方未安装本模组 1.2 以上版本"，不把误判当结论。
- `[异常]` 标签改名 `[跨区]` 并与 `[断开]` 同色；聚合行保留"异常"（它统计的是所有不在语音房的人）。
- 没装 mod 的玩家不显示任何延迟（延迟只能靠 mod 广播）。
- 简易模式三行 + 房主的新人通知（成功留 3s / 失败留 10s）。
- 延迟改走 3 秒一次的 Unreliable 事件（`TYPE_PING = 3`），不再等 30 秒的属性更新。
- 缓存快照三组：决策与重连计数、语音房双口径+幽灵数、房间码/语音房名/强制区服。
- **诊断日志**：本机语音区服变化、语音房名变化、孤立翻转、房间语音服来源变化、队友跨区翻转、面板模式切换、配置变更——各自只在变化时记一行，进 BepInEx 日志 + Alt+J 控制台，不广播。换房时清空去重快照。

### 1.6.0 — 房间码兜底 + 邀请握手

- **B1**：`CodeToRegion('-')` 兜底为可配置区服（默认 hk）。做成**幂等**（只在结果仍是 `us` 时才改）+ `[HarmonyAfter("com.github.LengSword.BetterRoomShare")]`，所以与 BetterRoomShare 任意组合结果一致。配置留空 = 保持原版行为。
- **B2**：切区后等 `ConnectedToMasterServer` 再 `JoinRoom`，20 秒超时。同样幂等（已连上时第一次检查即通过）。
- **B3**：邀请握手每 8 秒重发、最多 4 次，全失败时说明两个可能原因。实现是反射把本体的 `m_currentlyRequestingRoomID` 置回 `None`，本体下一帧自然重发。**全程 fail-open**：字段找不到就停用并记日志。
- 三项各有独立开关（默认开），与其他 mod 冲突时可单独关。
- 补 1.4.0 漏改的两个本地化键：配置项这才真正改名成「机场常驻简易UI」。
- 修"简易 UI 显示为空"：新显示规则与旧的"单人 10 秒清空内容"重叠，导致 canvas 开着而内容为空。删掉旧那层。
- 顶部拆成两行（房间区服 / 房间语音服），一行放不下两个 IP。
- **房主视角不做装饰**：这游戏一切以房主为准，房主连的就是标准答案，所以房主看自己的顶部行和第二行**不上色、无 √/×、无"本机推定"**。例外：房主自己语音断开或孤立时仍给警告。
- 打本体补丁一律走 `TryPatch` fail-open（2.4.b 的 `SteamManager` 搬家就害得老 mod 抛 `TypeLoadException`）。

### 1.6.1 — 细节修正（当前版本）

- `✓`/`✗`(U+2713/2717) 游戏字体无字形、渲染成豆腐块 → 换成 `√`/`×`(U+221A/00D7)。
- **孤立判定加前提**：队友还在连接时不能判"语音房内只有本机"。现在要求至少有一个别人确实自报了非空 IP 才作数。
- 字体层级：表头 100%、玩家列表 85%、第二行 70%、缓存快照 75%（与顶部一致）。
- 面板移除「测速缓存」行（Alt+J 的「区服状态」里仍有）。
- **决策行只在客机显示**（决策链只在客机侧跑），且未介入时显示"尚未介入"而不是拿初值凑出假的"跟房主 → 盲连"；「多数派区服」移到决策行下面。
- 语音房名 ≠ `房间码_voice_` 时，**房间码与语音房名双双飘红**。
- 「多数玩家在另一频道」警告改判据：原本走 `PlayerCache`（含本机自己、30 秒才刷一次），强制重连瞬间会把"3 秒前的自己"算成另一个人而误报。现在用 `GetRoomVoiceServer`（只统计别人、实时读属性），且要求本机确实连上语音。

---

## 4. 设计决策与不变量（**不要当 bug 改**）

1. **`DecideTargetIP` 是多数优先**（多数派 count≥2 → 跟随房主 → 盲连），不是房主优先。理由：≥2 个独立客机同时跟错到同一台的概率≈0，比"单信房主"更稳（房主一人跟歪会带偏全队）。混装环境下更是刚需——房主装旧版会跑偏，多数派才是对的。
2. **主机没装 mod 不算"失效"**：游戏自带 Photon 语音服会自匹配，"强制跟房主 IP"只是叠加增强；拿不到房主 IP 就退多数派/盲连，正常。
3. **"跟随"的实现方式变了，但功能没退化。** 塞 IP 那个动作从未生效；真正让"跟随"成立的是「区服 + 房名」两个信息一致后 Photon 自动路由。修好区服绑定后，判据（发现走散）+ 执行（用正确参数重连）第一次同时有效，mod 从"只会检测"变成"能自我纠正"。
4. **✓/× 判据是 IP 比较，不是区服比较。** 同区也能连错（多集群 / 旧房名残留）。
5. **「房间语音服」只信别人的自报。** 本机自己的地址永不作为权威答案，否则本机连错时会把错的显示成对的。
6. **聚合与每行同源**：`[同步:n/N]` 与每行状态标签都来自同一次 `ClassifyPlayers`，杜绝"同步却报错位""连着却报孤立"的矛盾。
7. **符号一律不变色**：`[` `]` `(` `)` `/` `-` `»` 全部 C_TEXT，只有内容按状态上色。√/× 一律在括号前。
8. **房主视角不做装饰**：房主是金标准，自己给自己打勾无意义。
9. **本机自己有问题时，对别人的判定不可信，必须明说**（"未知：本机断开语音服且对方未安装本模组 1.2 以上版本"）。
10. **兜底本体 bug 一律幂等 + fail-open**：只在结果仍是错的时候才动手，反射/类型找不到就停用并记日志，绝不让附加功能拖垮整个 mod。

## 5. 未完成 / 未验证

| 项 | 状态 |
|---|---|
| **BetterRoomShare 共存实测** | ❌ 它仍在 `BepInEx/LengSword-BetterRoomShare-0.1.0/`（**不在 `plugins/` 下，BepInEx 不扫这个路径**），日志里从未加载。共存逻辑是理论正确（幂等 + HarmonyAfter），没实测过 |
| 1.2.1 测速修复 | ⚠️ 未验证（Alt+J →「测各区延迟」应 5 秒内出表） |
| 区服强制的连不上提示 / 延迟质量体检 | ⚠️ 未验证 |
| 1.6.x 全部面板改动 | ⚠️ 未验证（尤其字体层级与对齐） |
| 跨区房实测 | ⚠️ 需要两台不同区服的机器；1.1.0 的验证是单机同区，无法证伪 A1（但"三次全 eu 而语音缓存是 ussc"已经排除了"重连丢区服"） |
| 邀请握手重试 | ⚠️ 需要真实 Steam 邀请 + 房主不在房里的场景才能触发 |
| C1 网络可达性（港服要加速器） | ❌ **修不了**，只能提示。进不去房这一步与语音无关 |
| 配置重置 | 1.4.0 换过分区、1.6.0 改过键名，这两次都会让相关配置回到默认值（BepInEx 按 Section+Key 定位） |

## 6. 关键路径与命令

```
源码        F:\Games\SteamLibrary\steamapps\common\PEAK\MODs\PeakVoiceFixFix\
部署        F:\...\PEAK\BepInEx\plugins\0核心&基础模组\[优化]更好的语音修复CN-<版本>\
配置        F:\...\PEAK\BepInEx\config\chuxiaaaa.Aiae.BetterPeakVoiceFix.cfg
BepInEx日志  F:\...\PEAK\BepInEx\LogOutput.log
Unity日志   F:\...\PEAK\output_log.txt        （不是 LocalLow，那里的 Player.log 是旧的）
区服缓存    注册表 HKCU\Software\LandCrab\PEAK → PUNCloudBestRegion / VoiceCloudBestRegion
```

构建：`dotnet build -c Release`（dotnet 10）。**版本号硬编码 4 处必须同步**：`PeakVoiceFix/VoiceFix.cs` 的 `PLUGIN_VERSION`、`manifest.json`、`csproj` 的 `<Version>`、`README.md` 首行。csproj 的 PostBuild 会把 DLL 拷到 `BepInEx/plugins/` **根目录**，每次部署后要删掉那份（同 GUID 会冲突）。

源码文件（`PeakVoiceFix/`）：

| 文件 | 职责 |
|---|---|
| `VoiceFix.cs` | 插件入口、配置绑定、Harmony 注册、`TryPatch` fail-open |
| `NetworkManager.cs` | 连接决策、重连、属性/事件广播、语音房映射、诊断日志 |
| `VoiceUIManager.cs` | 两套面板渲染、Alt+J 控制台、玩家分类 |
| `RegionControl.cs` | 强制区服、各区测速、区服状态报告 |
| `RoomCodePatches.cs` | B1 房间码负号兜底、B2 切区等主服务器 |
| `InviteHandshake.cs` | B3 邀请握手超时重发 |
| `Localization.cs` | 中英双语字典（`L.Get`），`L.IsChinese` |
| `Patches.cs` / `PhotonRPCFix.cs` | 186 事件接管、骨架 RPC 修复 |

## 7. 本会话新增的反编译档案

都在 `MODs\反编译\`，命名 `<程序集>_<日期>_v<游戏版本>`：

- `PhotonVoicePUN_2026-09-04_v2.4.b`（`PunVoiceClient`，本体连语音的正确姿势就在 `ConnectVoice()`）
- `Photon_Portningsbolaget_2026-09-04_v2.4.b`（**2.4.b 新增的 `Photon.dll`**，含 `SteamPhotonAuthManager` 等平台认证层）
- `PhotonRealtime_2026-09-04_v2.4.b`（复核 `GameServerAddress` / `ReconnectAndRejoin` / `ClientState` 枚举；与 4 月旧档逐字节相同）
- `Utilities_Portningsbolaget_2026-09-04_v2.4.b`（**区服编码表就在这**，`REGIONS` 缺 hk 的铁证）
- `BetterRoomShare_0.1.1_2026-09-05`（第三方，LengSword 的房间码增强）

已有档案里这次重点用到：`Assembly-CSharp_PEAK_2026-09-03_v2.4.b`（`VoiceClientHandler` / `MainMenuJoinRoomPage` / `SteamLobbyHandler` / `NetworkConnector` / `NetworkingUtilities`）、`PhotonVoice`（`VoiceConnection` 在 `Photon.Voice.Unity\`，`VoiceFollowClient` 在 `Photon.Voice\`）。

## 8. 临时文件

`MODs\PeakVoiceFixFix\_ui_preview.html` —— 面板设计预览（带真实配色的 HTML），下划线前缀表示可删。本报告定稿的样式与它基本一致，差异是 1.6.1 的几项（符号换 √×、顶部两行、房主无装饰、字体层级）。

